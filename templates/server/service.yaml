apiVersion: v1
kind: Service
metadata:
  name: {{ include "sgroups-chart.fullname" . }}
  labels:
    {{- include "sgroups-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sgroups-chart.selectorLabels" . | nindent 4 }}
