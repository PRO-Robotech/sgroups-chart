{{- $intTLS    := .Values.internalTLS -}}
{{- $expose    := .Values.expose -}}

{{- if or (eq $intTLS.certSource "certmanager") (and $expose.enabled (eq $expose.tls.certSource "certmanager")) }}
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "sgroups-chart.fullname" . }}-selfsigned
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sgroups-chart.labels" . | nindent 4 }}
spec:
  selfSigned: {}

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "sgroups-chart.fullname" . }}-ca
  namespace: {{ .Release.Namespace }}
spec:
  commonName: sgroups-ca
  isCA: true
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: {{ include "sgroups-chart.fullname" . }}-selfsigned
  privateKey:
    algorithm: ECDSA
    size: 256
  secretName: {{ include "sgroups-chart.fullname" . }}-ca-secret

---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "sgroups-chart.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  ca:
    secretName: {{ include "sgroups-chart.fullname" . }}-ca-secret
{{- end }}
